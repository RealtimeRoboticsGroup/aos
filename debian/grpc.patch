diff --git a/bazel/copts.bzl b/bazel/copts.bzl
index 3044ce85c5..59f0fc23d6 100644
--- a/bazel/copts.bzl
+++ b/bazel/copts.bzl
@@ -159,5 +159,5 @@ GRPC_LLVM_WINDOWS_WARNING_FLAGS = GRPC_LLVM_WARNING_FLAGS + [
 GRPC_DEFAULT_COPTS = select({
     "//:use_strict_warning": GRPC_LLVM_WARNING_FLAGS + ["-DUSE_STRICT_WARNING=1"],
     "//:use_strict_warning_windows": GRPC_LLVM_WINDOWS_WARNING_FLAGS + ["-DUSE_STRICT_WARNING=1"],
-    "//conditions:default": [],
+    "//conditions:default": ["-Wno-missing-constinit"],
 })
diff --git a/bazel/grpc_extra_deps.bzl b/bazel/grpc_extra_deps.bzl
index 6ac6093a5f..00b3ded113 100644
--- a/bazel/grpc_extra_deps.bzl
+++ b/bazel/grpc_extra_deps.bzl
@@ -56,9 +56,9 @@ def grpc_extra_deps(ignore_version_differences = False):
 
     api_dependencies()
 
-    go_rules_dependencies()
-    go_register_toolchains(version = "1.22.5")
-    gazelle_dependencies()
+    #go_rules_dependencies()
+    #go_register_toolchains(version = "1.22.5")
+    #gazelle_dependencies()
 
     # Pull-in the go 3rd party dependencies for protoc_gen_validate, which is
     # needed for building C++ xDS protos
