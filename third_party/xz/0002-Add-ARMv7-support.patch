diff --git a/BUILD.bazel b/BUILD.bazel
index 31b2d3f..e8f0d5c 100644
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -37,6 +37,17 @@ config_setting(
     },
 )
 
+config_setting(
+    name = "linux_armv7",
+    constraint_values = [
+        "@platforms//os:linux",
+        "@platforms//cpu:armv7",
+    ],
+)
+
 config_setting(
     name = "linux_riscv64",
     constraint_values = [
@@ -84,6 +95,17 @@ config_setting(
     template = "config.lzma-linux.h",
 )
 
+expand_template(
+    name = "config_linux_armv7",
+    out = "config.lzma-linux-armv7.h",
+    substitutions = {
+        "{HAVE_IMMINTRIN_H}": "#undef HAVE_IMMINTRIN_H",
+        "{HAVE_CPUID_H}": "#undef HAVE_CPUID_H",
+        "{HAVE_USABLE_CLMUL}": "#undef HAVE_USABLE_CLMUL",
+    },
+    template = "config.lzma-linux.h",
+)
+
 expand_template(
     name = "config_linux_x86_64",
     out = "config.lzma-linux-x86_64.h",
@@ -126,6 +148,7 @@ copy_file(
     src = selects.with_or({
         "@platforms//os:android": "config.lzma-android.h",
         ":linux_arm64": "config.lzma-linux-arm64.h",
+        ":linux_armv7": "config.lzma-linux-armv7.h",
         ":linux_riscv64": "config.lzma-linux-riscv64.h",
         ":linux_x86_64": "config.lzma-linux-x86_64.h",
         ":osx_arm64": "config.lzma-osx-arm64.h",
