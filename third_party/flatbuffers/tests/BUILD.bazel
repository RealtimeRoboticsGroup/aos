# This package is intentionally empty.
#
# The "upstream" targets that used to live here dependend on AOS-modified
# flatbuffers_cc_library / flatbuffers_py_library macros. Since those macros now
# live in AOS, keeping the upstream test targets would require this file to
# load AOS-owned macros. Stub these for now.
#
# Once flatbuffers is fully migrated to bzlmod, the upstream tests should be
# executable from the AOS workspace without any modifications.
package(default_visibility = ["//visibility:private"])
